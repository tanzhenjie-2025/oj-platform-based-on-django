# ==============================
# 1. 敏感配置文件（绝对不能提交）
# ==============================
.env                  # 存储数据库密码、密钥等敏感信息
.env.local            # 本地环境的额外配置（若有）


# ==============================
# 2. Python 自动生成的缓存/垃圾文件
# ==============================
__pycache__/          # Python 字节码缓存目录
*.py[cod]             # 字节码文件（.pyc、.pyo、.pyd）
*$py.class            # 其他字节码格式
.pytest_cache/        # 若用 pytest 测试，忽略测试缓存
.coverage             # 测试覆盖率报告文件（若有）


# ==============================
# 3. Django 本地环境文件（无需提交）
# ==============================
db.sqlite3            # Django 本地默认数据库（生产用不上，且含本地数据）
db.sqlite3-journal    # 数据库日志文件
staticfiles/          # 第三方静态资源（Bootstrap、DRF 等，可通过 collectstatic 重新生成）
media/                # 若有用户上传文件（如图片、附件），也忽略（生产通常用云存储）
django_debug.log      # Django 日志文件（若开启了日志输出）


# ==============================
# 4. 虚拟环境（依赖已在 requirements.txt 记录）
# ==============================
venv/                 # 常见的虚拟环境目录（若你用的是这个）
env/                  # 另一种常见虚拟环境目录
.venv/                # 隐藏的虚拟环境目录（部分开发者习惯）
ENV/                  # 大写版本的虚拟环境目录


# ==============================
# 5. 编辑器/IDE 配置文件（个人环境，不共享）
# ==============================
.vscode/              # VS Code 配置（含插件、格式化规则等）
.idea/                # PyCharm 配置目录
*.sublime-project     # Sublime Text 项目配置
*.sublime-workspace
.DS_Store             # Mac 系统自动生成的隐藏文件
Thumbs.db             # Windows 系统自动生成的缩略图文件


# ==============================
# 6. Docker 相关临时文件（非核心配置）
# ==============================
docker-data/          # 若本地挂载 Docker 数据卷（如数据库数据），忽略此目录
*.dockerignore.bak    # .dockerignore 的备份文件（若有）
docker-compose.override.yml  # 本地调试用的 Compose 覆盖配置（若有，不共享）


# ==============================
# 7. 其他临时文件（通用忽略）
# ==============================
logs/                 # 项目日志目录（若有，含动态输出的日志）
tmp/                  # 临时文件目录（若项目生成临时文件）
*.log                 # 所有日志文件（若日志散落在根目录）
.DS_Store             # Mac 系统文件（再次强调，避免遗漏）